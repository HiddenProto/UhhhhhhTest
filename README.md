# UhhhhhhTest

Public loadable build of the edited Uhhhhhh reanim script (for executor testing).
Analysis + master reference live in a separate private repo.

```lua
loadstring(game:HttpGet("https://raw.githubusercontent.com/HiddenProto/UhhhhhhTest/main/reanim.lua"))()
```
